import { OfferFilterPipe } from './offer-filter.pipe';

describe('OfferFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OfferFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
